import { createTheme } from '@mui/material';

export const themeOptions = createTheme({
    palette: {
        primary: {
            main: '#2B3844',
            darkblue: '#202C36',
            white: '#F2F2F2',
        },
        divider: '#292929'
    }
});